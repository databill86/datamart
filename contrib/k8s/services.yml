---
apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  labels:
    app: datamart
    what: elasticsearch
spec:
  selector:
    app: datamart
    what: elasticsearch
  ports:
  - protocol: TCP
    port: 9200
---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  labels:
    app: datamart
    what: rabbitmq
spec:
  selector:
    app: datamart
    what: rabbitmq
  ports:
  - protocol: TCP
    port: 5672
---
apiVersion: v1
kind: Service
metadata:
  name: coordinator
  labels:
    app: datamart
    what: coordinator
spec:
  selector:
    app: datamart
    what: coordinator
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 8001
    targetPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: query
  labels:
    app: datamart
    what: query
spec:
  selector:
    app: datamart
    what: query
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 8002
    targetPort: 8002
